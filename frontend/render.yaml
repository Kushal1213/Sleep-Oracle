services:
  - type: web
    name: sleep-oracle-frontend
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    envVars:
      - key: VITE_API_URL
        value: https://sleep-oracle-backend.onrender.com
